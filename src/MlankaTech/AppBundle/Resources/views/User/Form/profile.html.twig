{{ form_row(form.title) }}

{{ form_row(form.firstName) }}

{{ form_row(form.lastName) }}

{{ form_row(form.gender) }}

{{ form_row(form.group) }}
<br />
<div class="form-group">
    <div class="pull-right">
        <a href="{{ path('mlanka_tech_app.user_edit',{slug:user.slug}) }}.html" type="submit" class="btn btn-primary">Edit</a>
    </div>
</div>
{{ form_rest(form) }}