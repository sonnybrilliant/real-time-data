{% extends "::base.html.twig" %}

{% block header %}
    <div class="page-head">
        <h2>{{ page_header }}</h2>
        <ol class="breadcrumb">
            {% if is_granted('ROLE_USER_LIST') %}
                <li><a href="{{ path('mlanka_tech_app.user_list')}}.html" title="List users" alt="List users">Users</a></li>
                <li class="active">{{ breadcrumb }}</li>
            {% else  %}
                <li>Users</li>
                <li class="active">{{ breadcrumb }}</li>
            {% endif %}
        </ol>
    </div>
{% endblock header %}


{% block body %}

{% endblock body %}
