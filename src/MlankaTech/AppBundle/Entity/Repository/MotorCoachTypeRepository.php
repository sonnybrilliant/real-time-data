<?php

namespace MlankaTech\AppBundle\Entity\Repository;

use Doctrine\ORM\EntityRepository;
/**
 * MotorCoachTypeRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class MotorCoachTypeRepository extends EntityRepository
{
    /**
     * Get query of all train types
     *
     * @param array $options
     * @return Query
     */
    public function getAllQueryList($options)
    {

        $defaultOptions = array(
            'search' => '',
            'filterBy' => '',
            'sort' => 'm.id',
            'direction' => 'desc',
            'show' => 10,
        );

        foreach ($options as $key => $values) {
            if (!$values) {
                $options[$key] = $defaultOptions[$key];
            }
        }

        $qb = $this->createQueryBuilder('m')->select('m');

        // search
        if ($options['search']) {
            if ($options['search'] != "") {
                $qb->andWhere($qb->expr()->orx(
                    $qb->expr()->like('m.type', $qb->expr()->literal('%' . $options['search'] . '%'))
                ));
            }
        }

        $qb->orderBy($options['sort'], $options['direction']);
        return $qb->getQuery();
    }
}